package com.example.foot.repository;

import com.example.foot.model.SponsorModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SponsorRepository extends JpaRepository<SponsorModel, Integer> {
}
